import React from 'react';

import { Content, Heading, Context} from './styles';

const Home = props => {
    return (
        <Content>
            <Heading>React Templates</Heading>
            <Context>A Starter kit project tools with React Libraries and others tools supported.</Context>
        </Content>
    );
};

export default Home;
